package _07_oop_2.hw;

interface Payment extends Comparable<Employee>{
	////////////////////////////////////////////////////////////////////////////
	double calculatePay();
	////////////////////////////////////////////////////////////////////////////
	@Override
	default int compareTo(Employee empl) {
		return -Double.compare(this.calculatePay(), empl.calculatePay());
	}
	////////////////////////////////////////////////////////////////////////////
}