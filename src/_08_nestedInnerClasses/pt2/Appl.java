package _08_nestedInnerClasses.pt2;

class Appl {
	////////////////////////////////////////////////////////////////////////////
	//	Create Wrapper class which should wrap any objects
	//	which implements Shape interface
	////////////////////////////////////////////////////////////////////////////
	public static void main(String[] args) {
		new Wrapper<Shape>(new Circle());
		new Wrapper<Shape>(new Square());
		}
	////////////////////////////////////////////////////////////////////////////
}